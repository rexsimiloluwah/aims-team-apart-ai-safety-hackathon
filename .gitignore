# Planning / context docs (Claude-generated, not part of the repo)
claude-docs/

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Environments
.venv/
.env

# uv
# (uv.lock IS committed; .venv is not)

# Experiment artifacts
# artifacts/ is now COMMITTED so teammates get results/figures/tables on clone.
# (Run logs are still excluded via *.log below; results CSVs, predictions, and figures are kept.)
wandb/
*.log

# Notebooks
.ipynb_checkpoints/

# Model / HF caches
.cache/
hf_cache/

# LaTeX build artifacts (keep main.tex + figures/; main.pdf optional)
*.aux
*.fls
*.fdb_latexmk
*.out
*.toc
*.synctex.gz

# OS / editor
.DS_Store
.idea/
.vscode/
*.swp
